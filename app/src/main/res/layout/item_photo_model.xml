<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#00FFFF00"
    android:gravity="center">

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/img_id_image"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:adjustViewBounds="true"
        android:src="@drawable/image_intro_1"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <LinearLayout
        android:id="@+id/linearLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:gravity="center"
        android:orientation="vertical"
        app:layout_constraintTop_toBottomOf="@+id/img_id_image">

        <ImageView
            android:id="@+id/img_dot1"
            android:layout_width="76dp"
            android:layout_height="8dp"
            android:src="@drawable/img_dot1"
            android:visibility="visible"
            app:tint="@color/color_main" />

        <ImageView
            android:id="@+id/img_dot2"
            android:layout_width="76dp"
            android:layout_height="8dp"
            android:src="@drawable/img_dot2"
            android:visibility="gone"
            app:tint="@color/color_main" />

        <ImageView
            android:id="@+id/img_dot3"
            android:layout_width="76dp"
            android:layout_height="8dp"
            android:src="@drawable/img_dot3"
            android:visibility="gone"
            app:tint="@color/color_main" />
    </LinearLayout>

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tv_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_marginHorizontal="52dp"
        android:layout_marginTop="30dp"
        android:gravity="center"
        app:fontFamily="@font/bariol_regular"
        android:includeFontPadding="false"
        android:text="@string/content_onboarding_1"
        android:textColor="@color/black"
        android:textSize="@dimen/sp_16"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/linearLayout" />

</androidx.constraintlayout.widget.ConstraintLayout>